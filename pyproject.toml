[project]
name = "lerobot-ur5e-gello-dev-env"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.11"

dependencies = []

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
    "black",
]